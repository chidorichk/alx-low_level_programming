#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 *_abs - check whether a letter is lower or not
 *@n: parameter c allow to handle the character
 *Return: 1 or 0 or -1
 */

int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
}
